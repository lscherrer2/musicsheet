/**
 * File and directory naming constants (client-side reference)
 */
export const FILE_NAMES = {
  PDF: 'score.pdf',
  METADATA: 'metadata.json',
  THUMBNAIL: 'thumbnail.png',
  INDEX: 'index.json',
  CONFIG: 'config.json',
} as const;

export const DIR_NAMES = {
  DOCUMENTS: 'documents',
} as const;
